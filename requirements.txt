# Core
numpy>=1.24
pandas>=2.0
Pillow>=9.5
PyYAML>=6.0
scipy>=1.10

# Simulation / optical GA
colour-science>=0.3
opencv-python>=4.8
matplotlib>=3.7
scikit-image>=0.21
tqdm>=4.60
toml>=0.10

# 3D / NIfTI
nibabel>=5.0

# ML / SDXL inpainting (optional — needed for LoRA training only)
torch>=2.2
diffusers>=0.27
mlflow>=2.0
unidecode>=1.3
timm>=1.0

# DVC — reproducibility for submission-time artifact tracking
dvc>=3.0
